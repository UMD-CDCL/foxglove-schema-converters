# CDCL Tracking Schema Converter

Foxglove schema converters for CDCL active-track messages.

This extension provides GeoJSON output for `TrackArray` and standard
`NavSatFix`/`Vector3` outputs for `TrackState`.
